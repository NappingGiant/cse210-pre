using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello Develop04 World!");

        // exceeds requirements:
        //  code to use all prompts and questions before randomizing the order again
        //  keeps track of total time spent in the activity (as opposed to requested time)
        //  verifies valid value for duration input

        Mindfulness foo = new Mindfulness(); // start the app (constructor runs it)
    }
}